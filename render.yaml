services:
  - type: web
    name: crypto-analysis-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn test:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11